[build-system]
requires = ["setuptools>=42.0.0", "wheel"] # PEP 508 specifications.
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
line_length = 79
